import React, {PureComponent} from 'react';

class CartItemComponent extends PureComponent {
  render() {
    // const temp = JSON.stringify(this.props.cartItem.prop)
    return (
        <div>

        </div>
    );
  }
}

export default CartItemComponent;